# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its developer tools focus without claiming live deployment or external usage.

## Cases

- `baseline`: `change width`, score 153, lane `ship`
- `stress`: `diagnostic quality`, score 169, lane `ship`
- `edge`: `review cost`, score 202, lane `ship`
- `recovery`: `safe rewrite`, score 207, lane `ship`
- `stale`: `change width`, score 153, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
